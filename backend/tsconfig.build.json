{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    // Hérite des options de base, mais override si besoin pour le build
    "outDir": "./dist",  // Ou ton dossier de sortie
    "removeComments": true  // Optionnel : nettoie pour prod
  },
  "include": [
    "src/**/*"
  ],
  "exclude": [
    "node_modules",
    "**/*.test.ts",
    "**/*.spec.ts",
    "__tests__/**",
    "__mocks__/**",
    "vitest.config.ts",
    "dist"
  ]
}